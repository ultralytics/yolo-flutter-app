/// An example app demonstrating how to use the `ultralytics_yolo` plugin.
///
/// This app launches a simple Flutter screen (`CameraInferenceScreen`)
/// that uses the YOLO object detection model to perform inference
/// on camera frames in real time.
library;

/// Entry point for the example.
/// This simply delegates to the shared app logic.
void main() => main();
